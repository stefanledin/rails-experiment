<h1>Home#index</h1>
<p class="lead">Find me in app/views/home/index.html.erb</p>
<h3>Mina resor</h3>
<%= link_to 'Lägg till resa', new_trip_path %>
<% if @presets %>
	<h3>Favoriter</h3>
	<ul>
		<% @presets.each do |preset| %>
			<li><%= link_to preset.label, new_preset_path %></li>
			<%= debug preset %>
		<% end %>
	</ul>
<% end %>

